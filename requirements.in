
# Core authentication and Azure libraries (use minimum versions; allow resolver to pick latest compatible)
msal>=1.34.0
requests>=2.32.5
azure-identity>=1.25.0
azure-core>=1.35.1
azure-keyvault-secrets>=4.10.0
# Keep azure-mgmt-monitor at least 7.x but allow future minor updates
azure-mgmt-monitor>=7.0.0,<8.0.0
azure-common>=1.1.28
azure-mgmt-core>=1.6.0

# Environment variable management
python-dotenv>=1.1.1

# Data processing (if needed)
# Allow compatible minor upgrades for pandas/numpy
pandas>=2.3.0
numpy>=2.3.0

